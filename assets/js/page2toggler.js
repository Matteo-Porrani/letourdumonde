
// const page2CountryList = document.querySelector('.page2__country__list');
// déjà déclaré...


/*

const page2toggler = document.querySelector('.toggler--page2');

page2toggler.addEventListener('click', (e) => {
  
  console.log(e.target.classList);

  if (e.target.dataset.state === 'closed') {
    e.target.dataset.state = 'open';
    page2CountryList.classList.remove('country__list--hidden');
  } else if (e.target.dataset.state === 'open') {
    e.target.dataset.state = 'closed';
    page2CountryList.classList.add('country__list--hidden');
  }


});


*/
